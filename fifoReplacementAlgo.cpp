//FCFS DISK SCHEDULING
#include<iostream>
using namespace std;
int main(){
    int n,frameSize,hit,fault;
    cout<<"Enter the number of references: ";
    int referenceValue[n];
    cout<<"\n Enter the value of reference: ";
    for(int i=0;i<n;i++){
        cin>>referenceValue[i];
    }
    cout<<"\nEnter the frame size: ";
    cin>>frameSize;
    int frame[frameSize];
    for(int i=0;i<frameSize;i++){
        if(frames[]){
            
        }
    }
}

